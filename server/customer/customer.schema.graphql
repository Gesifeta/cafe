type Query {
  customers: [Customer]
}
type Customer {
  id: ID
  firstName: String
  lastName: String
  gender: String
  address: Address
  favorites: [Menu]
}
type Address {
  street: String
  city: String
  country: String
}
